// Generated by GitHub Copilot
import { Component, OnInit } from '@angular/core';
import { Router } from '@angular/router';
import { QuizService } from '../../services/quiz.service';
import { ExamResult } from '../../models/quiz.model';

/**
 * Result page displaying exam score breakdown after submission.
 */
@Component({
  selector: 'app-result',
  standalone: true,
  templateUrl: './result.component.html',
  styleUrl: './result.component.css',
})
export class ResultComponent implements OnInit {
  result: ExamResult = { total: 0, attempted: 0, correct: 0, wrong: 0, score: 0 };

  constructor(
    private quizService: QuizService,
    private router: Router
  ) {}

  ngOnInit(): void {
    this.result = this.quizService.evaluateExam();
  }

  /**
   * Navigates to the review page to see all answers.
   */
  reviewAnswers(): void {
    this.router.navigate(['/review']);
  }

  /**
   * Resets all state and returns to the papers list.
   */
  retakeQuiz(): void {
    this.quizService.reset();
    this.router.navigate(['/papers']);
  }
}
